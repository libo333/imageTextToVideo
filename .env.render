# ============================================
# Render.com 环境变量配置清单
# ============================================
# 📋 部署到 Render 时,需要在控制台配置以下环境变量
# 路径: Dashboard -> 你的服务 -> Environment -> Environment Variables

# ==================== 必需配置 ====================

# Telegram Bot Token (从 @BotFather 获取)
TELEGRAM_BOT_TOKEN=你的_Bot_Token

# Web App 公网地址 (Render 自动分配的域名 + /webapp)
# 例如: https://your-service-name.onrender.com/webapp
WEBAPP_URL=https://your-service-name.onrender.com/webapp

# API 基础地址 (与上面相同,但不加 /webapp)
API_BASE_URL=https://your-service-name.onrender.com

# ==================== ComfyUI 配置 ====================
# ⚠️ 重要: ComfyUI 需要通过 Cloudflare Tunnel 暴露到公网
# 详见: docs/deploy/cloudflare-tunnel-guide.md

# ComfyUI 主服务地址 (通过 Cloudflare Tunnel 获得的公网地址)
# 例如: https://your-tunnel.trycloudflare.com
COMFYUI_URL=https://your-comfyui-tunnel.trycloudflare.com

# ComfyUI API 服务地址 (与上面相同)
COMFYUI_API_URL=https://your-comfyui-tunnel.trycloudflare.com

# SVD API 地址 (如果使用独立 API,否则与上面相同)
SVD_API_URL=https://your-comfyui-tunnel.trycloudflare.com

# CogVideoX API 地址 (如果使用独立 API,否则与上面相同)
COGVIDEO_API_URL=https://your-comfyui-tunnel.trycloudflare.com

# ==================== 服务器配置 ====================

# 端口 (Render 会自动设置,通常是 10000)
PORT=10000

# 环境
NODE_ENV=production

# API 端口配置
SVD_API_PORT=3002
COGVIDEO_API_PORT=3003

# 输出目录
OUTPUT_DIR=./output

# ==================== 可选配置 ====================

# 阿里云 API (如果使用)
# ALIYUN_API_KEY=your_aliyun_api_key
# ALIYUN_REGION=beijing

# ==================== 配置步骤 ====================

# 1. 在 Render Dashboard 中打开你的服务
# 2. 点击左侧 "Environment"
# 3. 点击 "Add Environment Variable"
# 4. 逐个添加上述环境变量
# 5. 点击 "Save Changes"
# 6. 服务会自动重新部署

# ==================== 获取 Render 域名 ====================

# 部署完成后,Render 会提供一个免费域名,格式为:
# https://your-service-name.onrender.com
#
# 你需要:
# 1. 复制这个域名
# 2. 在 WEBAPP_URL 后面加上 /webapp
# 3. 更新 Telegram Bot 的菜单按钮 URL (在 @BotFather 中设置)

# ==================== Cloudflare Tunnel 设置 ====================

# 在本地机器上运行 ComfyUI,然后使用 Cloudflare Tunnel:
#
# 1. 安装 cloudflared:
#    Windows: winget install cloudflare.cloudflared
#    Mac: brew install cloudflared
#    Linux: 见官网文档
#
# 2. 运行 tunnel:
#    cloudflared tunnel --url http://localhost:8188
#
# 3. 复制生成的公网 URL (例如: https://xxx.trycloudflare.com)
#
# 4. 将这个 URL 填入上面的 COMFYUI_URL 等变量中
#
# 详细步骤见: docs/deploy/cloudflare-tunnel-guide.md
